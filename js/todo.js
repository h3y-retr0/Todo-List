
document.addEventListener('DOMContentLoaded', () => {

    
    const addTodo = () => {
        if(input.value.lenght === 0) {
            return
        } else {
            // Add todo
        }
    }
})